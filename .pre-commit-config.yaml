repos:
  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: uv run pytest --cov=timesaver --cov-report=term-missing --cov-fail-under=100
        language: system
        types: [python]
        pass_filenames: false
        always_run: true
